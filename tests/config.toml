[mozci]
verbose = 1
data_sources = ["hgmo", "errorsummary", "taskcluster", "treeherder_client", "bugbug"]

[mozci.cache]
default = "file"
retention = 1000

[mozci.cache.stores]
file = { driver = "file", path = "mozci_tests_cache" }
